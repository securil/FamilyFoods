---
const certifications = [
  {
    name: "FSSC 22000",
    description: "글로벌 식품 안전 표준 인증",
    icon: "/images/certification-fssc.svg"
  },
  {
    name: "HACCP",
    description: "식품안전관리인증기준",
    icon: "/images/certification-haccp.svg"
  },
  {
    name: "ISO 9001",
    description: "품질경영시스템 인증",
    icon: "/images/certification-iso9001.svg"
  }
];

const processSteps = [
  {
    title: "원료 선별",
    description: "엄격한 기준으로 최상의 원재료만을 선별합니다."
  },
  {
    title: "생산 공정",
    description: "첨단 설비와 체계적인 공정으로 안전하게 생산합니다."
  },
  {
    title: "품질 검사",
    description: "철저한 품질 검사를 통해 제품의 안전성을 보장합니다."
  },
  {
    title: "배송",
    description: "신선한 상태로 고객에게 제품이 전달될 수 있도록 관리합니다."
  }
];
---

<section id="quality" class="section-padding bg-gradient-to-b from-white to-family-gray">
  <div class="container-custom">
    <h2 class="section-title">
      <span class="text-family-orange">품질</span> 관리 시스템
    </h2>
    <p class="text-center text-gray-600 max-w-3xl mx-auto mb-12">
      훼미리식품은 엄격한 품질관리 시스템을 통해 안전하고 맛있는 제품을 생산합니다.
      국제 표준에 부합하는 인증을 획득하여 식품 안전에 최선을 다하고 있습니다.
    </p>
    
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-16">
      {certifications.map((cert, index) => (
        <div 
          data-aos="fade-up" 
          data-aos-delay={index * 100}
          class="bg-white p-6 rounded-lg shadow-md text-center"
        >
          <img 
            src={cert.icon || "/images/certification-placeholder.svg"} 
            alt={cert.name} 
            class="w-24 h-24 mx-auto mb-4"
          />
          <h3 class="text-xl font-bold mb-2">{cert.name}</h3>
          <p class="text-gray-600">{cert.description}</p>
        </div>
      ))}
    </div>
    
    <div class="bg-white p-8 rounded-lg shadow-md">
      <h3 class="text-2xl font-bold mb-8 text-center">생산 프로세스</h3>
      
      <div class="relative">
        <!-- 연결선 -->
        <div class="absolute top-1/2 left-0 w-full h-1 bg-gray-200 -translate-y-1/2 hidden md:block"></div>
        
        <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
          {processSteps.map((step, index) => (
            <div class="relative flex flex-col items-center text-center z-10">
              <div class="w-16 h-16 rounded-full bg-family-orange text-white flex items-center justify-center text-xl font-bold mb-4">
                {index + 1}
              </div>
              <h4 class="text-lg font-bold mb-2">{step.title}</h4>
              <p class="text-gray-600">{step.description}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>
